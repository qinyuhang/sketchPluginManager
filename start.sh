#!/usr/bin/env bash
echo "start.sh running"
if [ -d GitHubAPIKey.sh ]; then
    sh GitHubAPIKey.sh
    echo "exported keys"
eles
    echo "Not set GitHub API Key Will have access limit!"
fi
npm run start